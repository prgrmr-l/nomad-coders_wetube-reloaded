extends base.pug


block content 
    h4 Change Title of video
    form(method="POST")
        input(palceholder="Video Title",value=video.title required)
        input(value="Save",type="submit")
        